<div class="all-products">
  <section class="wrapper">
    <div class ="container-data">
      <div>
        <h1 class="navbar-title heading">comfortfit </h1>
      </div>
      <div class="content">
        <div class="container">
          <div class="row">
            <% @products.each do |product| %>
              <div class="col-xs-12 col-sm-4">
                <div class="card">
                    <%= link_to product, class: "img-card" do %>
                      <%# if product.images.attached? %>
                        <%#= image_tag url_for(product.photo) %>
                      <%# end %>
                      <% if product.images.attached? %>
                          <%= image_tag(product.images[2]) %>
                      <%end%>
                    <%end %>
                    <div class="card-content">
                      <h4 class="card-title">
                        <%= link_to product.name, product, alt: "product image" %>
                      </h4>
                      <h5 class=""> <b>Color:</b>
                        <%= product.color %>
                      </h5>
                      <h5 class=""><b>Precio:</b>
                        <span>$</span><%= product.price %><span>.MX</span>
                      </h5>
                      <h5 class=""><b>Size: </b>
                        <span>$</span><%= product.size %><span>.MX</span>
                      </h5>
                      <h5 class=""><b>Cantidad en existencia:</b>
                        <%= product.stock %>
                      </h5>
                    </div>
                    <div class="card-read-more">
                      <%= link_to 'Ver Producto', product, class: "btn btn-light text-dark " %>
                    </div>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>